require "spec_helper"

describe Activeantipattern do
  it "has a version number" do
    expect(Activeantipattern::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
